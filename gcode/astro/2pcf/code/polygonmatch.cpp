#include<iostream>
#include<cmath>
#include<fstream>
#include<cstring>
#include<ctime>
#include<cstdlib>
#include "tools.h"

using namespace std;

int main()
{
    time_t timebegin, timeend;
    timebegin = time(NULL);

    long number_galaxy, number_polygon, number_galaxy_selected;
    double racen[number_polygon];
    polygon *polygon_array;
    polygon_array = (polygon*)malloc(sizeof(polygon)*number_polygon);
\\ read in the data of galaxy sample
\\ read in the data of polygon
    timeend = time(NULL);
}
